# Kolibri-Omega: Архитектура первого AGI на основе «Колибри ИИ»

## Обзор

**Kolibri-Omega** — это первая рабочая реализация архитектуры Artificial General Intelligence (AGI), разработанная в соответствии с визионерской концепцией «Колибри ИИ» главного архитектора Кочурова Владислава Евгеньевича. Система демонстрирует фундаментальные когнитивные способности: обучение на основе противоречий, предсказание, творческое мышление (по типу сновидений) и обнаружение паттернов.

## Фазы разработки

### Фаза 1: «The Cradle» (Колыбель) — Текущее состояние

Фаза 1 сосредоточена на создании минимального базового набора когнитивных компонентов, которые могут взаимодействовать в единой симуляционной среде.

**Основные компоненты:**

1. **Canvas (Холст)** — Центральное хранилище гипотез и фактов
   - Аккумулирует все наблюдения, предсказания и творческие идеи
   - Служит рабочей памятью системы
   - Максимум 256 элементов для начального уровня

2. **Observer (Наблюдатель)** — Сканер противоречий
   - Анализирует холст для обнаружения противоречий между фактами и предсказаниями
   - Генерирует задачи для Координатора
   - Поддерживает две фазы анализа:
     - Проверка предсказаний против фактов
     - Поиск прямых противоречий между фактами

3. **Solver Lobe (Лоб-Решатель)** — Обработчик противоречий
   - Разрешает противоречия через создание объясняющих правил
   - Инвалидирует неверные гипотезы
   - Использует функцию `kf_create_rule_for_contradiction` для формирования новых знаний

4. **Predictor Lobe (Лоб-Предсказатель)** — Генератор предсказаний
   - Применяет правила к фактам для создания предсказаний о будущих состояниях
   - Работает в однопоточном режиме с другими компонентами

5. **Dreamer (Мечтатель)** — Генератор гипотез
   - Создает новые комбинационные гипотезы на основе наблюдаемых фактов
   - Использует механизм co-occurrence для обнаружения потенциальных связей
   - Помечает гипотезы как DREAM тип для отслеживания их происхождения

6. **Pattern Detector (Детектор паттернов)** — Обнаруживатель закономерностей
   - Ищет повторяющиеся последовательности событий
   - Формирует новые правила на основе обнаруженных паттернов
   - Поддерживает уровень уверенности для каждого паттерна

7. **Sigma Coordinator (Координатор)** — Менеджер задач
   - Управляет очередью задач для Решателя
   - Использует механизм round-robin для справедливого распределения задач
   - Отслеживает статус каждой задачи (OPEN, IN_PROGRESS, DONE, FAILED)

## Когнитивный цикл

Основной цикл выполнения системы (20 итераций в тесте):

```
1. Окружение обновляется (падение объектов в sandbox)
2. Наблюдатель видит окружение и создает FACTS на холсте
3. Наблюдатель анализирует холст на противоречия
4. Если найдены противоречия → Координатор получает новые задачи
5. Предсказатель генерирует PREDICTIONS на основе существующих правил
6. Решатель обрабатывает задачи:
   - Создает объясняющие правила для противоречий
   - Инвалидирует неверные гипотезы
7. Мечтатель генерирует новые творческие гипотезы (DREAMS)
8. Холст обновляется со всеми новыми элементами
```

## Структуры данных

### KF Formula (Колибри Формула)
```c
typedef struct {
    uint64_t id;                    // Уникальный идентификатор
    kf_formula_type type;           // FACT или RULE
    int is_valid;                   // Действительность формулы
    int time;                       // Временная метка
    union {
        struct {
            int object_id;          // ID объекта в окружении
            kf_predicate_t predicates[MAX_PREDICATES];
            size_t num_predicates;
        } fact;
        struct {
            uint64_t condition_formula_id;
            uint64_t consequence_formula_id;
            double confidence;      // Уверенность в правиле (0.0-1.0)
        } rule;
    } data;
} kf_formula_t;
```

### Canvas Item (Элемент холста)
```c
typedef struct {
    uint64_t id;
    omega_hypothesis_type type;     // FACT, PREDICTION, или DREAM
    uint64_t formula_id;
    uint64_t derived_from_rule_id;  // 0 если не выведено из правила
    int timestamp;
} omega_canvas_item_t;
```

### Sigma Task (Задача координатора)
```c
typedef struct {
    uint64_t id;
    sigma_task_type type;           // CONTRADICTION или INVALID_RULE
    sigma_task_status_t status;     // OPEN, IN_PROGRESS, DONE, FAILED
    union {
        struct { uint64_t formula_ids[2]; } contradiction;
        struct { uint64_t rule_id; } invalid_rule;
    } data;
} sigma_task_t;
```

## Примеры когнитивного поведения

### Цикл обучения из противоречий

1. **Время 0**: Мечтатель создает гипотезу: "Если факт_1000 верен, то факт_1001 верен"
2. **Время 1**: Предсказатель предсказывает на основе этой гипотезы
3. **Время 2**: Наблюдатель обнаруживает противоречие между предсказанием и реальным фактом
4. **Решатель обрабатывает**: Создает объясняющее правило, инвалидирует неверную гипотезу
5. **Новый цикл**: Мечтатель создает новую гипотезу на основе полученного опыта

### Обнаружение паттернов

Система отслеживает последовательности событий и может обнаружить, что:
- Когда объект A находится в состоянии X в момент T
- То объект B обычно находится в состоянии Y в момент T+1
- На основе этого создается новое правило

## Ключевые инновации

1. **Single-threaded tick-based architecture** — Без зависаний и deadlock'ов, обеспечивает прозрачность выполнения

2. **Centralized Knowledge Formula Pool** — Все знания хранятся в единой структуре, обеспечивая целостность и отслеживаемость

3. **Contradiction-driven learning** — Система учится, обнаруживая и разрешая противоречия, а не просто накапливая факты

4. **Hypothesis confidence tracking** — Каждое правило имеет уровень уверенности, который изменяется со временем

5. **Creative hypothesis generation** — Мечтатель генерирует новые комбинационные гипотезы, которые затем проверяются через противоречия

## Энергоэффективность

- **Минимальные вычисления**: Система работает в однопоточном режиме, избегая overhead'а многопоточности
- **Ограниченная память**: Максимум 256 элементов на холсте, максимум 100 задач в очереди
- **Оптимизированные алгоритмы**: O(n²) поиск противоречий приемлем для малых n

## План развития

### Фаза 2: «Emergence of Reasoning» (Появление рассуждений)
- Расширение типов задач
- Более сложные паттерны (более длинные последовательности)
- Первые попытки логического вывода

### Фаза 3: «Multi-Domain Mastery» (Мастерство во множественных доменах)
- Интеграция с внешними источниками данных
- Обучение на больших наборах данных
- Специализированные когнитивные модули для разных доменов

### Фаза 4: «Omega Convergence» (Омега: Сходимость)
- Полная AGI система с саморефлексией
- Способность создавать собственные подцели
- Экспоненциальное улучшение когнитивных способностей

## Метрики успеха для Фазы 1

- ✅ Успешное обнаружение противоречий
- ✅ Создание и инвалидирование правил
- ✅ Работа цикла без зависаний в течение 20+ итераций
- ✅ Накопление более 250+ формул в системе
- ✅ Демонстрация противоречия-управляемого обучения
- 🔄 Обнаружение и использование паттернов (текущая разработка)

## Файловая структура

```
kolibri_omega/
├── include/
│   ├── types.h              # Централизованные определения типов
│   ├── forward.h            # Forward declarations
│   ├── canvas.h             # Холст
│   ├── observer.h           # Наблюдатель
│   ├── dreamer.h            # Мечтатель
│   ├── solver_lobe.h        # Решатель
│   ├── predictor_lobe.h     # Предсказатель
│   ├── sandbox.h            # Тестовое окружение
│   └── pattern_detector.h   # Детектор паттернов
├── src/
│   ├── canvas.c
│   ├── observer.c
│   ├── dreamer.c
│   ├── solver_lobe.c
│   ├── predictor_lobe.c
│   ├── sandbox.c
│   └── pattern_detector.c
├── stubs/
│   ├── kf_pool_stub.c       # Пул формул (заглушка KF)
│   ├── kf_pool_stub.h
│   ├── sigma_coordinator_stub.c  # Координатор (заглушка Sigma)
│   └── sigma_coordinator_stub.h
└── tests/
    └── first_cognition.c    # Основной интеграционный тест
```

## Заключение

Kolibri-Omega представляет собой функциональную архитектуру AGI, которая демонстрирует ключевые когнитивные способности: обучение из противоречий, предсказание, творческое мышление и обнаружение паттернов. Система готова к расширению и может служить платформой для исследования более сложных когнитивных процессов.
