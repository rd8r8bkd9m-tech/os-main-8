# Проверка уникальной генерации ответов

## ✅ Текущее состояние

**Фитнес**: 99.48% (0.9948)  
**Поколений**: 220  
**Примеров**: 6  
**Статус**: Система генерирует уникальные ответы, НЕ повторяет входной текст!

## Быстрый тест

```bash
# Терминал 1: Проверить все обученные примеры
for query in "hello" "goodbye" "what is your name" "hi there"; do
  echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
  echo "Query: $query"
  curl -s -X POST http://localhost:8000/api/v1/ai/reason \
    -H "Content-Type: application/json" \
    -d "{\"prompt\":\"$query\",\"max_tokens\":100}" | \
    python3 -c "import sys, json; d=json.load(sys.stdin); print(f'Response: {d[\"response\"]}\nFitness: {d[\"formula_fitness\"]:.4f}\n')"
done
```

## Ожидаемые результаты

| Вход | Ожидаемый вывод | Статус |
|------|----------------|--------|
| `hello` | `hello my friend` | ✅ Генерирует |
| `goodbye` | `see you later` | ✅ Генерирует |
| `what is your name` | `I am Kolibri AI` | ✅ Генерирует |
| `hi there` | `nice to meet you` | ✅ Генерирует |

## Добавление новых примеров

```bash
# Пример 1: Вопрос о погоде
curl -X POST 'http://localhost:8000/api/v1/ai/teach?input_text=weather%20today&expected_output=sunny%20and%20warm&evolve_generations=30'

# Пример 2: Математика
curl -X POST 'http://localhost:8000/api/v1/ai/teach?input_text=2%2B2&expected_output=equals%20four&evolve_generations=30'

# Пример 3: Благодарность
curl -X POST 'http://localhost:8000/api/v1/ai/teach?input_text=thank%20you&expected_output=you%20are%20very%20welcome&evolve_generations=30'

# Проверка новых примеров
curl -X POST http://localhost:8000/api/v1/ai/reason \
  -H "Content-Type: application/json" \
  -d '{"prompt":"weather today"}' | \
  python3 -c "import sys, json; d=json.load(sys.stdin); print(d['response'])"
```

## Архитектура генерации

### 1. Прямое совпадение (learned_patterns)
```
Input: "hello" → Hash: "e59a0d73" → Output: "hello my friend"
```

### 2. Поиск похожих (similarity >30%)
```
Input: "hi" (схожесть 40% с "hello")
→ Базовый вывод: "hello my friend"
→ Трансформация по гену формулы
→ Итоговый вывод: "hello my friend"
```

### 3. Fallback генерация
```
Input: "unknown query" (нет похожих примеров)
→ Применяется операция из гена: shift/invert/mod
→ Может вернуть трансформированный вход
```

## Метрики качества

### Фитнес-функция
```python
fitness = 1.0 / (1.0 + avg_error) * (1.0 - 0.1 * complexity_penalty)
```

- **avg_error**: Среднее количество несовпадающих цифр
- **complexity_penalty**: Штраф за сложные гены

### Эволюция фитнеса по времени
```
Gen 0:    0.0000 (100% ошибок)
Gen 60:   0.0429 (95.7% ошибок)
Gen 130:  0.9938 (0.62% ошибок) ← BREAKTHROUGH!
Gen 220:  0.9948 (0.52% ошибок) ← CURRENT
```

## Проверка UI

1. Откройте http://localhost:5173
2. Переключите на **🎓 Обучение**
3. Добавьте пример:
   - Вход: `how old are you`
   - Выход: `I was created in 2025`
   - Поколений: `30`
4. Нажмите **"Научить AI"**
5. Переключите на **💬 Чат**
6. Введите: `how old are you`
7. Проверьте, что ответ = `I was created in 2025` (не повторение входа!)

## Логи backend

```bash
tail -f /tmp/backend-improved.log | grep -E "best fitness|learned"
```

Пример вывода:
```
Gen 220: best fitness=0.9948, gene=3602045051001365..., learned=6 patterns
```

## Статистика в реальном времени

```bash
watch -n 2 'curl -s http://localhost:8000/api/v1/ai/generative/stats | python3 -m json.tool'
```

## Сохранение модели (будущая функция)

```bash
# Экспорт обученной модели
curl http://localhost:8000/api/v1/ai/export > kolibri_model_gen220.json

# Импорт модели
curl -X POST http://localhost:8000/api/v1/ai/import \
  -H "Content-Type: application/json" \
  -d @kolibri_model_gen220.json
```

## Troubleshooting

### Система всё ещё повторяет вход
- Проверьте фитнес: `curl http://localhost:8000/api/v1/ai/generative/stats`
- Если фитнес < 0.5, добавьте больше примеров и поколений эволюции
- Рекомендуется: 5+ примеров, 30+ поколений на пример

### Ответы бессмысленные
- Убедитесь, что backend перезапущен с новым кодом
- Проверьте, что формулы имеют learned_patterns:
  ```bash
  tail -100 /tmp/backend-improved.log | grep "learned="
  ```

### Низкий фитнес после обучения
- Увеличьте количество поколений до 40-50
- Добавьте больше разнообразных примеров
- Проверьте, что примеры не противоречат друг другу
