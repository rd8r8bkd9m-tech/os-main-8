# Kolibri OS

Колибри OS — легковесная экспериментальная платформа, объединяющая KolibriScript, симулятор и набор утилит для отладки цифровых сценариев, а также **Kolibri AI** — реальная, верифицируемая система искусственного интеллекта для локальных, автономных приложений.

Дополнительно включает **Kolibri-Omega** — продвинутую систему когнитивных рассуждений (AGI) на чистом C с многошаговым логическим выводом, категоризацией знаний и адаптивным обучением.

## 🤖 Kolibri AI — Реальная AI система

Kolibri AI — это **гибридная система рассуждений**, объединяющая:
- ✅ **Символьное рассуждение** — детерминированные правила KolibriScript
- ✅ **Нейронная инференция** — опциональный локальный LLM (ollama/llama.cpp)
- ✅ **Криптографическая верификация** — HMAC-SHA256 подпись всех решений
- ✅ **Полная трассировка** — audit trail каждого решения
- ✅ **Энергоэффективность** — 60-80% экономии против всегда-LLM

**📖 Полная документация**: [KOLIBRI_AI_IMPLEMENTATION.md](KOLIBRI_AI_IMPLEMENTATION.md) · [AGI_MANIFESTO.md](projects/kolibri_ai_edge/AGI_MANIFESTO.md)

### Быстрый пример

```python
from backend.service.ai_core import KolibriAICore

ai = KolibriAICore(secret_key="my-key", enable_llm=False)
decision = await ai.reason("Что такое фотосинтез?")

print(f"Ответ: {decision.response}")
print(f"Уверенность: {decision.confidence:.1%}")
print(f"Энергия: {decision.energy_cost_j} J")
print(f"Верифицировано: {decision.verify_signature('my-key')}")
```

### API endpoints

```bash
# Единичное рассуждение
POST /api/v1/ai/reason {"prompt": "..."}

# Пакетная обработка
POST /api/v1/ai/reason/batch {"queries": [...]}

# Статистика
GET /api/v1/ai/stats
```

---

## 🧠 Kolibri-Omega — Система когнитивных рассуждений (AGI)

**Статус:** Phase 2 In Progress (50% завершено)

Kolibri-Omega — это **экспериментальная AGI система** на чистом C, реализующая продвинутые когнитивные функции:

### Phase 1: Основные когнитивные компоненты ✅ ЗАВЕРШЕНО
- **Canvas** — рабочая память для фактов и гипотез
- **Observer** — наблюдение за миром и обнаружение противоречий
- **Predictor** — однопошаговые и многопошаговые предсказания
- **Dreamer** — генерирование конкретных и обобщенных гипотез
- **Solver** — решение противоречий и задач
- **Learning Engine** — адаптивное обновление уверенности
- **Pattern Detector** — обнаружение двухшаговых корреляций
- **Sigma Coordinator** — управление задачами и приоритетами

### Phase 2: Расширенное логическое рассуждение 🔄 IN PROGRESS

#### ✅ Завершено:
1. **Inference Engine** — многошаговое forward chaining
   - Обнаружение цепочек правил (A→B, B→C → обнаружить A→C)
   - Вычисление уверенности через произведение
   - Автоматическое создание shortcut-правил

2. **Abstraction Engine** — категоризация и обобщение
   - Автоматическая категоризация фактов (POSITION, MOTION, STATE, RELATIONSHIP)
   - Создание обобщенных правил (применимы ко всем членам категории)
   - Улучшенная переносимость правил между объектами

#### 🔄 В разработке:
3. **Self-Reflection Module** — анализ качества правил
4. **Extended Pattern Detection** — обнаружение паттернов 3+ шагов

### Быстрый запуск Kolibri-Omega

```bash
cd /Users/kolibri/Downloads/os-main\ 8
make test-omega
```

**Ожидаемый вывод:**
```
[Dreamer] Initialized.

--- Simulation Time: 0 ---
[Observer] Observing world at time 0.
[Dreamer] Dreamt a new rule 1002 based on co-occurrence
[AbstractionEngine] Discovered category 'POSITION_FACT' with 2 members

--- Simulation Time: 1 ---
[InferenceEngine] Found inference chain of length 1: 1000 ⟹ 1001 (confidence: 0.1000)

... (20 итераций)
```

**📊 Метрики:**
- Память: ~50-60 KB (стабильна)
- Производительность: ~10-12 ms на цикл
- Canvas items: 74+ за 4 временных шага
- Ошибок: 0, Deadlocks: 0

**📖 Подробная документация:**
- [PHASE_2_IN_PROGRESS.md](PHASE_2_IN_PROGRESS.md) — архитектура и дорожная карта
- [PHASE_2_PART_1_COMPLETE.md](PHASE_2_PART_1_COMPLETE.md) — отчет о завершении

## Требования
- Python 3.10+
- `pip` и `virtualenv`
- Компилятор C/C++ с поддержкой CMake 3.20+
- Ninja либо Make (по желанию)

## Быстрый старт
1. Клонируйте репозиторий и перейдите в директорию проекта.
2. Подготовьте виртуальное окружение Python (поддерживаются версии 3.10+):
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # Windows: .venv\\Scripts\\activate
   python -m pip install --upgrade pip
   ```
3. Установите инструменты, перечисленные в [`requirements.txt`](requirements.txt). Файл включает точные версии `pytest`, `coverage`, `ruff` и `pyright`, которые используются в CI.
   ```bash
   pip install -r requirements.txt
   ```
4. Соберите C-компоненты Kolibri:
   ```bash
   cmake -S . -B build -G "Ninja"  # или опустите -G, чтобы использовать Makefiles
   cmake --build build
   ```

5. Для веб-интерфейса соберите wasm-ядро перед фронтендом:
   ```bash
   ./scripts/build_wasm.sh
   ```
6. Соберите фронтенд (после установки npm-зависимостей в `frontend/`):
   ```bash
   cd frontend
   npm install
   npm run build
   ```
7. Запустите тесты:

   ```bash
   pytest -q
   ruff check .
   pyright
   ctest --test-dir build
   ```

### Быстрая подготовка релиза

- Создайте виртуальное окружение и установите зависимости:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

- Прогоните проверки перед сборкой релиза:

```bash
.venv/bin/pytest -q    # ожидается: 120 passed
.venv/bin/ruff check .
.venv/bin/pyright
```

См. `RELEASE.md` для подробных шагов упаковки и публикации.

## Проверки качества
- Линтеры Python: `ruff check`, `pyright`
- Политики проекта: `python scripts/policy_validate.py`
- Форматирование C-кода выполняется стандартными средствами компилятора; следуйте существующему стилю файлов в `apps/` и `tests/`.

## Дополнительные ресурсы
- [План релиза](docs/project_plan.md) описывает долгосрочные вехи и критерии готовности.
- Скрипты и утилиты размещены в `scripts/`; каждый скрипт содержит встроенные подсказки по использованию.

## Режимы ответа Kolibri
Kolibri OS поддерживает два режима генерации ответов:

1. **Deterministic KolibriScript** — ответы собираются локально внутри браузера через WebAssembly-мост. Это режим по умолчанию.
2. **LLM Proxy** — запросы проксируются в внешний LLM через FastAPI-сервис.

Чтобы активировать режим LLM, задайте переменные окружения и запустите сервис:

```bash
export KOLIBRI_RESPONSE_MODE=llm
export KOLIBRI_LLM_ENDPOINT="https://llm.example.com/v1/infer"
export KOLIBRI_LLM_API_KEY="<token>"  # опционально
export KOLIBRI_LLM_MODEL="kolibri-pro"  # опционально
export KOLIBRI_LLM_TEMPERATURE="0.7"  # опционально, диапазон 0.0-2.0
export KOLIBRI_LLM_MAX_TOKENS="512"  # опционально, положительное целое
./scripts/run_backend.sh --port 8080
```

Фронтенд ожидает те же настройки через Vite:

```bash
export VITE_KOLIBRI_RESPONSE_MODE=llm
export VITE_KOLIBRI_API_BASE="http://localhost:8080/api"
npm --prefix frontend run dev
```

Если `VITE_KOLIBRI_RESPONSE_MODE` не равен `llm`, интерфейс автоматически вернётся к KolibriScript. При ошибке LLM фронтенд повторит запрос через KolibriScript и дополнит ответ примечанием о деградации.

